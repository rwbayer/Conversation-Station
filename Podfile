# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Conversation Station' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Conversation Station
    pod 'SpeechKit'

  target 'Conversation StationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Conversation StationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
